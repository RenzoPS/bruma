node_modules/

# Aca no va ningun .env: la configuracion del proyecto vive en el de la raiz.
# Las reglas quedan igual por las dudas, para que un archivo suelto creado a
# mano no se cuele en un commit.
.env
.env.local

# Tests y build
coverage/
*.tsbuildinfo
dist/

*.log
